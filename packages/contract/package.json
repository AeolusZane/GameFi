{
  "name": "contract",
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^0.15.2",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.3"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "ignition": "npx hardhat ignition deploy ignition/modules/Hero.ts --network localhost",
    "node": "npx hardhat node"
  },
  "dependencies": {
    "bignumber.js": "^9.1.2",
    "ethers": "^6.12.1"
  }
}